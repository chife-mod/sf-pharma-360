import { Dashboard } from "@/components/v2/dashboard";

/* v2 DOLs list — full dashboard. Composes Toolbar + Filter sidebar
 * + Influencer card grid + active-chip row + empty state. Header
 * and aurora bg live in the parent layout (app/concepts/v3/layout.tsx).
 */
export default function V2DolsPage() {
  return <Dashboard />;
}
